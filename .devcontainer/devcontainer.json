{
  "name": "Dawarich Dev",
  "dockerComposeFile": [
    "../docker/docker-compose.yml",
    "docker-compose.dev.yml"
  ],
  "service": "dawarich_app",
  "workspaceFolder": "/var/app",
  "customizations": {
    "vscode": {
      "extensions": [
        "rebornix.Ruby",
        "castwide.solargraph",
        "ms-azuretools.vscode-docker",
        "shopline.vscode-ruby-test-adapter"
      ],
      "settings": {
        "ruby.interpreter.commandPath": "bundle"
      }
    }
  },
  "forwardPorts": [3000, 5432, 6379], // Web, DB, and Redis
  "remoteUser": "root",
  "postCreateCommand": "bundle install && yarn install --ignore-engines && bundle exec rails db:prepare"
}