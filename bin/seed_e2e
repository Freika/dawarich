#!/usr/bin/env bash
# Reset demo/lite/family users and re-seed the canonical e2e fixture set.
# Wraps `rake e2e:reset_and_seed`. Run from the dawarich repo root, or
# from anywhere — the script cd's to the repo root.

set -euo pipefail

cd "$(dirname "$0")/.."

exec bin/rails e2e:reset_and_seed
